import symbolData from '@/data/aurora-symbol-paths.json';

export const AURORA_VIEWBOX = symbolData.viewBox;

/** Four lobes union into a symmetrical infinity / figure-8 (GSAP stagger reveal). */
export const AURORA_CLIP_SEGMENTS = symbolData.clipSegments;

/** Continuous infinity stroke (hero overlay, footer, decorative). */
export const AURORA_OUTLINE_PATH = symbolData.outlinePath;

/** Filled infinity silhouette (icons, watermarks). */
export const AURORA_FILLED_PATH = symbolData.filledPath;
